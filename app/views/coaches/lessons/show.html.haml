%h2 レッスンの個別ページ
%p
  = image_tag @lesson.thumbnail.thumb.url
  = "タイトル: #{@lesson.title}"
  = "内容: #{@lesson.description}"
  = "ジャンル: #{@lesson.genre.name}"
  = link_to "編集する", edit_coaches_lesson_path(@lesson.id)
  = link_to "削除する", coaches_lesson_path(@lesson.id), method: :delete, data: { confirm: "本当に削除してもいいですか？" }

%h2 受け持ち生徒
%p= link_to "生徒詳細はこちらから", coaches_lesson_students_path(@lesson)
- @rooms.each do |room|
  = link_to coaches_lesson_student_path(@lesson, room.student) do
    %p
      = room.student.email
      = image_tag room.student.avatar

  %h2.lesson-students 受講中の生徒
  - @rooms.each do |room|
    = link_to coaches_lesson_student_path(@lesson, room.student) do

      %p
        = room.student.email
        = image_tag room.student.avatar

    .article-thumbnail
      %span{ style: "background-image: url(#{room.student.avatar});"}
      .article-right-box
        .article-right-top-box.clearfix
          %span.lesson-duration
            2015.12.24 - 2015.12.30
          %span.lesson-genre{ style: "color: #D13B40; border-color: #D13B40"}
            野球
        %p.lesson-title= @lesson.title
        %p.lesson-coach テストタロウ

  %p= link_to "生徒詳細はこちらから", coaches_lesson_students_path(@lesson)

  .lesson-info
    .sub-title
      %p レッスン情報
    %table.table-content
      %tr
        %td.table-title 申し込み期限
        %td 2015/12/25
      %tr
        %td.table-title 現在の受講者
        %td 1人
      %tr
        %td.table-title 今までの受講者
        %td 10人
